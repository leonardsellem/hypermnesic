{
  "description": "hypermnesic auto-recall — surfaces relevant memory at prompt submit. Silent unless HYPERMNESIC_MCP_URL is configured and the prompt is memory-relevant; a token is optional for authed hook routes and not part of MCP OAuth wiring; never blocks a turn.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/hypermnesic_agent_hook.py --host claude",
            "timeout": 6
          }
        ]
      }
    ]
  }
}
