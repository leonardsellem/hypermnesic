{
  "description": "hypermnesic auto-recall — surfaces relevant memory at prompt submit. Silent unless configured (HYPERMNESIC_MCP_URL + HYPERMNESIC_MCP_TOKEN) and the prompt is memory-relevant; never blocks a turn.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/scripts/hypermnesic_agent_hook.py --host claude",
            "timeout": 6
          }
        ]
      }
    ]
  }
}
