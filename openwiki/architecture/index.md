# Files

- [Git-First Write Path](git-first-write-path.md) - The one sanctioned write traced end to end — path guard, diff-or-die frontmatter gate, path-scoped git commit and push, the index as a following projection, the append-only audit log, and the refusal contract that ties them together.
- [Architecture Overview](overview.md) - The whole mental model of hypermnesic — git-tracked markdown as the single source of truth, the search index as a disposable projection of HEAD, the read and git-first write paths, the two serving lanes, and which module owns what.
- [Read-Time Convergence](read-time-convergence.md) - How one shared converge() pass in front of every read catches the disposable index up to HEAD, closes a bounded slice of the dense lag, degrades instead of failing, and signals — never performs — a manual reindex.
- [Retrieval and Indexing](retrieval-and-indexing.md) - How committed markdown becomes a disposable SQLite projection, and how a query becomes ranked hits — ingestion and chunking, the FTS5, sqlite-vec and doc-surface lanes, reciprocal-rank fusion, dedup and git recency, the body-wikilink graph, and the exact degradation contract.
- [Write Guard and Security Model](write-guard-and-security-model.md) - What bounds the write surface — the blocklist default with the legacy allowlist as opt-in narrowing, the protected-path and governance-fence classes, within-repo path resolution, the single rule shared with folder discovery, single-writer locks, who is permitted to write, and the secret non-echo discipline behind the accepted-risk threat model.
