# Files

- [Configuration and Tunables](configuration-and-tunables.md) - Every knob and the consequence of changing it — environment variables and the gitignored .env, the credential lookup order and its secret-free status shape, the pinned embedding model and dimension, the convergence and folder-discovery bounds, write-zone tiers, and the cloud access-token TTL override.
- [Memory and Client Control](memory-and-client-control.md) - The engine-host owner surfaces over what is remembered and who may reach it — list, inspect, export, write-scope answers from the same guard the write path enforces, preview-then-apply forget and revert as git events, the summary-only audit view, and the secret-free OAuth grant store with its revocation path.
- [Provisioning and Diagnostics](provisioning-and-diagnostics.md) - The first-run and troubleshooting order for hypermnesic — the read-only local proof that comes before any endpoint concept, the non-mutating doctor/status checks and what each non-mutating state means, the fail-closed public-endpoint setup, role provisioning for single/master/client, the post-merge convergence hook, and the client next actions each state implies.
