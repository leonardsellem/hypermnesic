# Demo-asset frame-review sign-off log

Every **rendered binary** (GIF/PNG/SVG) under `media/` is reviewed by hand against
[`docs/guides/demo-asset-frame-review-checklist.md`](../docs/guides/demo-asset-frame-review-checklist.md)
before it is embedded in any README or posted anywhere. An asset is shippable only once
its row here reads **`pass`** (plan U3 gate G7). Text sources (`.tape`, captions) are
covered separately by the automatic `scripts/preflight_public_scan.py` text gate.

| Asset | Reviewer | Date | Result | Notes |
|---|---|---|---|---|
| `media/engine/hero-receipt-loop.gif` | Claude (agent) | 2026-06-18 | pass | **re-rendered (LS-1778) to fix a hidden-setup leak in the 2026-06-15 render**: its first frames showed the hidden `materialize`/`serve` scrollback + job PID because `clear` is a no-op in VHS's headless terminal (no terminfo) and VHS doesn't block on shell commands, so the clear buffered behind setup and ran after `Show`. Fixed with per-step VHS `Sleep`s + an ANSI `printf '\033[3J\033[2J\033[H'` clear against an idle shell before `Show`. Now 18.36s / 224 KB / ffprobe 25fps (tape requests 15fps); clean `demo$` start (no setup leak), fixture commit identity, semantic recall `(degraded=False)`; OCR sweep CLEAN (tesseract via stdin — leptonica's file reader is broken in this env). |
| `media/engine/index-recovery.gif` | Claude (agent) | 2026-06-18 | pass | **re-rendered (LS-1778) with the same hidden-setup-leak fix** (see hero row). Now 18.76s / 188 KB / ffprobe 25fps (tape requests 15fps); clean `demo$` start, markdown survives index deletion, rebuild visible, restored recall `(degraded=False)`; no host/path/token leaks; OCR sweep CLEAN. |
| `media/engine/benchmark-longmemeval.svg` | Claude (agent) | 2026-06-15 | pass | text/XML asset, auto-scanned by preflight; rendered + verified: 7 numbers match BENCHMARKS.md, reader·judge labeled, GPT-4.1-judge rows walled off (AE3); no leaks |
| `media/engine/claude-code-client.gif` | — | — | pending | **manual GUI capture** — screen-record Claude Code vs the loopback master; sanitize per the checklist (see connector-montage/README.md). Not `.tape`-scriptable. |
| `media/engine/connector-montage/one-endpoint-many-clients.png` | Claude (agent) | 2026-06-15 | pass | rendered from committed SVG; placeholder URLs only (`<your-host>.ts.net`, `127.0.0.1`); reviewed — no real host/path/token |
| `media/engine/carousel-hn/*` (3) | Claude (agent) | 2026-06-15 | pass | hero-loop frames (write, commit, recall); OCR-swept — no username, owner, home path, host, or token |
| `media/engine/carousel-localllama/*` (3) | Claude (agent) | 2026-06-15 | pass | montage + hero frames; placeholder URLs only; OCR-swept clean |
| `media/engine/carousel-selfhosted/*` (3) | Claude (agent) | 2026-06-15 | pass | `ls -R`/`cat`/`git log` stills; used `ls -R` not `ls -la` to avoid the owner/username column; OCR-swept clean |
| `media/companion/companion-hero.gif` | — | — | pending | **manual Obsidian GUI capture** (R7). Mechanism verified: a `commit_note` write through the master creates a real note + (with a `[[wikilink]]` body) a new graph edge. Staging in `companion/RECORDING.md`. |
| `media/companion/read-only-proof.gif` | Claude (agent) | 2026-06-18 | pass | **Scripted + composited (LS-1778).** vstack of the companion read-surface still (top: Obsidian thinking-mode panel showing the `read-only · wrote: false` badge — a passive read surface with no write affordance) over the VHS terminal-refusal panel (bottom: `read-only-proof.tape` drives the real MCP `commit_note` against protected paths → `CLAUDE.md` "agent-instruction file", `.git/config` "protected dir (.git/)"). 17.34s / 256 KB / 1100×1326. Both halves OCR-swept CLEAN (70k chars; no host/`/Users`/token; companion shows "tailnet", demo vault name only). Captured via Obsidian CLI `dev:screenshot` (installer 1.12.7) against the disposable `/tmp/companion-demo-vault`. |
| `media/companion/obsidianmd-still.png` | Claude (agent) | 2026-06-18 | pass | **Scripted Obsidian-CLI capture (LS-1778).** Obsidian graph view over the disposable `companion-demo-vault` with the agent-written note `ideas/atomic-notes.md` (created live via a real MCP `commit_note`, `[[wikilink]]`-connected) visible as a new graph node + the vault file tree. 1512px, OCR-swept CLEAN. Captured via `dev:screenshot`. |

> Reviewers: replace `—`/`pending` with your name, the date (`YYYY-MM-DD`), and `pass`
> (or `fail` + what leaked) once you have scrubbed every frame. Add rows for any new
> rendered asset not listed above.
