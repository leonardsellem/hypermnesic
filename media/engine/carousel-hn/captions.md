# Carousel — Hacker News / r/programming

**One claim:** an agent's memory write is a *real git commit* you can audit — not a
black-box vector store.

Frames reused from the hero receipt loop (`../hero-receipt-loop.gif`).

| Still | Caption |
|---|---|
| `01-agent-writes.png` | An agent writes a memory through the real MCP `commit_note` tool. |
| `02-real-commit.png` | The write produced a real git commit — `git log` shows it on top of history. |
| `03-fresh-recall.png` | A fresh session recalls it, cited to the source markdown file. |

Leak status: frames OCR-swept + reviewed — sanitized `demo$` prompt, fixture commit
identity, no host/path/token (see `../../.review-log.md`).
