# Carousel — r/LocalLLaMA

**One claim:** point any client at one self-hosted endpoint; the memory is your files,
and writes are real git commits.

| Still | Caption |
|---|---|
| `01-one-endpoint.png` | Claude Code, ChatGPT, Claude mobile — one endpoint, your tailnet (placeholder URL shown). |
| `02-real-commit.png` | A write through that endpoint is a real git commit, not a hidden DB row. |
| `03-grounded-recall.png` | Recall is grounded — every hit cites the source markdown file. |

`01` is the connector montage (`../connector-montage/one-endpoint-many-clients.png`);
`02`/`03` reuse the hero receipt-loop frames. Placeholder URLs only; OCR-swept + reviewed.
