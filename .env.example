# Copy to .env (gitignored) and fill in. The OpenAI key is read from the
# environment / this file only — never written to the index, audit log, or any
# structured output (U2 credential discipline).
OPENAI_API_KEY=
