# Gate-A → Phase-B rollout runbook (ready to execute on operator approval)

The exact **reversible** homelab steps to make Phase A live, in dependency order, each with its
rollback inverse and homelab-mirror note. **None of this has run** — it executes only after you
approve Gate A. The live `:8848` master, honcho, gbrain, and the vault are currently untouched.

> Secrets discipline (V9): every client secret is generated by `auth-add-client` straight into a
> `chmod 600` env file — never printed to a terminal, a log, or a committed file. The agent will
> not echo any secret value while running these.

## Pre-req — merge the branch
A persistent systemd unit must run the **installed** engine, not this feature-branch worktree.
Merge `claude/condescending-sutherland-4d9b29` → `dev` (via PR; `dev`/`main` are read-only to the
agent) and `uv tool install` / reinstall the engine on the homelab first.

## Step 1 — deploy the AS (new isolated service; zero impact on the live stack)
1. `mkdir -p ~/.config/hypermnesic-as && chmod 700 ~/.config/hypermnesic-as`
2. Enroll the RS introspection client + the two homelab identities (secrets → owner-only files):
   - `hypermnesic auth-add-client --state ~/.config/hypermnesic-as/state.json --client-id hypermnesic-rs --rs --secret-out ~/.config/hypermnesic-as/rs.env --resource https://homelab.<tailnet-host>.ts.net/mcp`
   - `… --client-id homelab-claude --scope read --scope write --secret-out ~/.config/hypermnesic-as/homelab-claude.env --resource https://homelab.<tailnet-host>.ts.net/mcp`
   - `… --client-id homelab-codex  --scope read --scope write --secret-out ~/.config/hypermnesic-as/homelab-codex.env --resource https://homelab.<tailnet-host>.ts.net/mcp`
   - **(operator, off-homelab):** `… --client-id mac` on the Mac's behalf, secret delivered to the Mac's secret store out of band.
3. Systemd user unit `hypermnesic-as.service`: `ExecStart=hypermnesic serve-auth --host 127.0.0.1 --port 8849 --public-url http://127.0.0.1:8849 --resource https://homelab.<tailnet-host>.ts.net/mcp --state ~/.config/hypermnesic-as/state.json --token-ttl 3600`. `systemctl --user enable --now hypermnesic-as`.
4. **Verify:** `curl -s http://127.0.0.1:8849/.well-known/oauth-authorization-server | jq .introspection_endpoint`.
- **Rollback:** `systemctl --user disable --now hypermnesic-as`; the live stack never depended on it.
- **Mirror:** new `gbrain-brain/projects/homelab/services/hypermnesic-as.md` + `LOG.md` entry.

## Step 2 — token-minting helper (steady-state token for the hook + clients)
A `hypermnesic-token@.service` + `.timer` per identity that mints a fresh access token from the
client_credentials secret before TTL and writes it to `HYPERMNESIC_MCP_TOKEN` in the host env the
hook/MCP client read. (Minting `POST /token grant_type=client_credentials resource=… scope=write`;
the secret stays in the `chmod 600` env file, the token in an owner-only file.) Never per-prompt.
- **Rollback:** disable the timer; tokens expire harmlessly.
- **Mirror:** documented in `hypermnesic-as.md`.

## Step 3 — install the plugin (homelab)
- Claude: add the in-repo marketplace, install the `hypermnesic` plugin.
- Codex: install via `.codex-plugin` (skills shared); the hooks fire `--host codex`.
- The plugin's `.mcp.json` points at `homelab.<tailnet-host>.ts.net/mcp` (post-Step-4) — or the direct
  authenticated bind until Step 4 lands.
- **(operator):** same on the Mac.
- **Rollback:** uninstall the plugin.
- **Mirror:** note in `services/hypermnesic.md`.

## Step 4 — companion-safe master cutover (the one live-service-affecting step)
**Decision required first (companion topology):**
- **Option 2 (recommended, now engine-supported):** serve the **read-only companion** from an
  auth-off bind (a read-only serve is Phase-1-compatible and loopback/read-exempt), and the
  **write master** auth-on. Per-tool write-scope (security fix `e55b75b`) means one auth-on
  endpoint can require only a base scope while `commit_note` rejects non-`write` principals — so a
  single auth-on endpoint with a `read`-scoped companion token also works *if* the companion gets
  OAuth (Option 1).
- **Option 1:** add a `read`-scoped OAuth token to the Obsidian companion (companion code change).
- **Option 3:** accept a brief companion outage and re-provision immediately.

**No-gap order (never leave the write master reachable auth-off, never lock out live agents):**
1. AS up (Step 1) + every consumer holds a verified token (Step 2 + companion per the chosen option).
2. Bring up the auth-on master on its bind (`serve --enable-write --host 100.64.0.55 --auth-issuer-url http://127.0.0.1:8849 --auth-resource-url https://homelab.<tailnet-host>.ts.net/mcp --required-scope write`, with `HYPERMNESIC_RS_CLIENT_ID/SECRET` in its env from `rs.env`) **while gbrain's endpoint still answers**.
3. Verify each consumer reaches it (real `tools/list`+`search`, authenticated).
4. **Then** flip `tailscale serve` `/mcp` → hypermnesic `100.64.0.55:8848` (tailnet-internal, not Funnel), leaving honcho's `/honcho` + `.well-known/*` routes intact.
- **Rollback (one inverse op):** flip `tailscale serve` back + restore gbrain's reach + revert the master unit to the Phase-1 auth-off read path. DB/gbrain intact.
- **Mirror:** `services/{hypermnesic,gbrain}.md` + `infrastructure/network.md` (the serve-map change) + `LOG.md`, **before** Gate A is declared closed.

## Step 5 — close Gate A
Re-run the Gate-A checklist live (incl. the **Mac 2nd-peer round-trip** — operator-pasted) and the
unauth-`commit_note`-401 / wrong-aud-401 / hook-silent-on-401 checks against the **hostname**.
Then Phase B (U6 parity → U7 content-distill cutover → U8 already done here → U13 sweep) begins.

---
*This runbook is a plan, not an execution. It is committed so the post-approval rollout is fast,
reviewable, and rollback-ready. The agent halts at Gate A until you approve.*
